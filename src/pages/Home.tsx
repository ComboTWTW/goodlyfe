import Hero from "../components/Hero";

const Home = () => {
    return (
        <div className="w-full flex flex-col items-center">
            <Hero />
        </div>
    );
};

export default Home;
